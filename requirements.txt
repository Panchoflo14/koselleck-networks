gensim>=4.3
leidenalg>=0.10
python-igraph>=0.11
networkx>=3.2
numpy>=1.26
scipy>=1.11
pandas>=2.1
scikit-learn>=1.3
lxml>=5.0
pyyaml>=6.0
matplotlib>=3.8
seaborn>=0.13
jupyter>=1.0
tqdm>=4.66
flask>=3.0
gunicorn>=22.0
anthropic>=0.86
analiticcl>=0.4.9  # OCR lexicon correction (src/ocr_refinement.py) - prebuilt wheels, no Rust toolchain needed
langdetect>=1.0.9  # foreign-language paragraph filter (src/parse_tcp.py's extract_text)
cupy-cuda12x>=14.0  # optional: GPU offload for network.py's similarity matmul (config.yml's network.use_gpu). Requires an NVIDIA GPU; not needed to run the rest of the pipeline.
duckdb>=1.1  # src/rag/build_store.py's embedded store for the Ask chatbot (src/rag/) and label auditor (src/label_judge.py)
